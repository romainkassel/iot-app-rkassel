apiVersion: v1
kind: Service
metadata:
  name: service-www
  namespace: dev
spec:
  selector:
    app: playground # Must match the label in the Deployment
  ports:
    - protocol: TCP
      port: 80          # Port exposed by the Service
      targetPort: 8888  # App port
  type: ClusterIP